---
tags:
  - code-map
  - tooling
status: current
---

# Tools and Tasks

> [!summary]
> Everything you do with the project, from opening the editor to running a
> check, is a named task in one file, [mise.toml](../../mise.toml). The tool
> `mise` also installs the exact Godot version the project needs, so you never
> call a separately installed Godot. Most checks are small GDScript programs
> in `scripts/tools/` that load the main scene, poke at it and print "ok" or
> "FAIL" lines; each has its own task.

## Setup

`mise install` fetches the pinned tools from [mise.toml](../../mise.toml):
Godot 4.7.2-stable, Node 26, and the Godot MCP server package. The file also
sets `GODOT_PATH` to the mise-managed Godot binary, because the MCP server
only looks there. Run tasks with `mise run <task>`; `mise tasks` lists them.

## Tasks

Tasks marked "import" first run the `import` task, so the `.godot/` cache is
up to date.

| Task | Depends on | What it does |
| --- | --- | --- |
| `editor` | | Opens the project in the Godot editor. |
| `run` | import | Runs the main scene. |
| `run-skeleton` | import | Runs the skeleton viewer scene, wireframe sector cubes around the camera ([[skeleton]]). |
| `run-walk` | import | Runs the world walk scene at seed 0: the 3³ sectors around the origin solved on worker threads, their MultiMesh buffers and collision faces built there and placed as they arrive (GridMaps with the panel's `use_gridmap`), and a capsule to walk them; F free fly, V first or third person ([[placement]]). |
| `import` | | Imports all assets headlessly and regenerates `.godot/`. |
| `check-scripts` | import | Parses every `.gd` file with `--check-only`, one Godot per CPU in parallel, and fails if any has errors. |
| `smoke` | import | Runs the main scene, then the skeleton viewer, the tile contact sheet and the world walk scenes, headlessly for 60 frames each (120 s timeout per run) and fails if the log contains `SCRIPT ERROR`, `ERROR:`, `Parse Error` or `invalid UID`, printing the offending lines. Catches scene wiring, missing resource and runtime load errors that parsing alone misses. |
| `check` | import, check-scripts, smoke, panel-check, skeleton-histogram, skeleton-stats, graph-check, graph-connectivity --quick, raster-check --quick, tileset-check, adjacency-check, tiles-check-fixtures, tileset-build-check, tiles-check-placeholder --quick, solver-check --quick, boundary-check --quick, gridmap-check, multimesh-check, walk-check | The pull request tier, under 2 minutes: the statistical tasks run on their reduced sample ([[#Check tiers]]). Then runs `jobs-check --quick` and `streaming-check --quick` on their own, one after the other (`mise run --skip-deps`), because they time the main thread and fail when the parallel tasks take its CPU, and loads the project headlessly in editor mode and quits. CI runs it on every pull request ([[ci-and-export]]). |
| `check-full` | the same tasks without `--quick` | The full tier, about 12 minutes: every statistical task at full size, then `jobs-check` and `streaming-check` on their own and the same headless editor load. CI runs it on pushes to `develop` and nightly. |
| `templates` | | Downloads the export templates for the pinned Godot version into `~/.local/share/godot/export_templates/`, skipping if present. |
| `export` | import | Exports a release build: `mise run export <preset> <output>`. |
| `export-debug` | import | Same as `export` with a debug build. |
| `release:notes` | | Prints the Unreleased section of [[CHANGELOG]], the notes of the next release. |
| `release:changelog-roll` | | `release:changelog-roll <version> [--date YYYY-MM-DD] [--dry-run]`: renames Unreleased to `<version> - <date>`, opens a fresh Unreleased section above it and commits `chore(release): <version>`. |
| `release:bump` | | `release:bump <patch\|minor\|major> [--push] [--dry-run]`: raises `config/version` in `project.godot`, commits `chore(release): start <new>`, pushes the current branch with `--push` and prints the new version. |
| `release:package` | | `release:package <linux\|macos>`: packs the exported build into `build/release/megastructure-<version>-linux-x86_64.tar.gz` or `-macos-universal.zip`; in the release workflow it fails if the tag does not match the version. |
| `release:release` | | Releases the version in `project.godot`: checks it runs on a clean, up-to-date `develop` with `gh` logged in, then calls `release:notes` and `release:changelog-roll`, tags `v<version>`, pushes, creates the GitHub release and calls `release:bump patch` and pushes. `--dry-run` prints every command instead. See [[releasing]]. |
| `wt:new` | | `wt:new <issue> <slug> [--base-dir DIR] [--herdr] [--dry-run]`: fetches, creates the worktree `<issue>-<slug>` on a new branch from `origin/develop` (default folder `~/.herdr/worktrees/megastructure`), trusts it with mise and prints its path. See [[maintaining]]. |
| `wt:rm` | | `wt:rm <branch> [--dry-run]`: refuses on uncommitted changes, an unfinished rebase or merge, or commits that are on no branch of origin; otherwise removes the worktree, deletes the branch and prunes. |
| `pr:status` | | Lists open pull requests with branch, merge state and check results. |
| `pr:merge` | | `pr:merge <number> [--no-rebase] [--timeout SECONDS] [--dry-run]`: rebases the PR's worktree onto `origin/develop` (aborting on conflict), pushes with `--force-with-lease`, waits for the `check` run of the pushed HEAD, merges with a merge commit and fast-forwards `develop` in the main checkout. See [[maintaining]]. |
| `mcp` | | Runs the Godot MCP server over stdio, used by `.mcp.json`. |
| `clean` | | Deletes `.godot/` and `build/`. |
| `hash-vectors` | import | Prints the hash reference table ([[hash]]). Headless. |
| `ui-params` | import | Prints the shader parameter groups the tweak panel discovers ([[tweak-ui]]). Headless. |
| `seed-check` | import | Tests the seed control and seed-reproducible frames. Opens a window. |
| `screenshot-check` | import | Tests the HUD screenshot. Opens a window. |
| `preset-check` | import | Tests that presets round-trip bit for bit. Headless. |
| `code-map-check` | import | Tests that every source file is linked from exactly one code map note. Headless. |
| `shot` | import | `shot <scene> <out.png> [--seed N] [--pose x,y,z,yaw,pitch] [--frames N] [--params k=v,...] [--resolution WxH] [--ui] [--window]`: renders one frame of a scene to a PNG under `xvfb-run` (X11, OpenGL) so no window opens; without `xvfb-run`, or with `--window`, it opens a normal window. See [[screenshots]]. |
| `skeleton-histogram` | import | Prints the seed 0 sector type histogram and tests `sector_type` against [[skeleton_histogram_seed0]]; `--update` rewrites the reference ([[skeleton]]). Headless; part of `check`. |
| `skeleton-stats` | import | Measures the sector grammar over 20 random 5³ regions per seed 0 to 4 and fails unless shafts run at least 3 sectors on average and solid splits a region into at least 2 non-solid components on average ([[sector-skeleton-and-walkable-graph]]). Headless; part of `check`. |
| `graph-check` | import | Checks the walkable graph's portals and interior nodes over 100 random adjacent open sector pairs per seed 0 to 4 ([[walkable-graph]]). Headless; part of `check`. |
| `graph-connectivity` | import | Checks the walkable graph's edges for every boundary scheme and tunnel weight variant over a random and a chasm-centred 15³ sample per seed 0 to 9 and fails unless every region-aligned 3³, 6³ and 9³ window has one component of open sectors; reports per variant the tunnel fraction, tunnels with a chasm or cavity end, strict 5³ windows and the mean vertical run ([[walkable-graph-connectivity]]). `--variants a,b` runs only some; `--quick` runs the reduced sample. Headless; part of `check`. |
| `raster-check` | import | Checks the edge rasteriser over 1 000 random sectors at seeds 0 to 4: determinism, no conflicting records, cells inside the sector, portal openings in both endpoint sectors, no rejected edge, and every walk changing level only on stairs and ladders; prints family counts and the stair cells level changes produce ([[edge-rasteriser]]). `--quick` runs the reduced sample. Headless; part of `check`. |
| `tileset-check` | import | Checks the tile resource format: the socket string grammar on every face, a prototype of every tile family, that the fixture tileset validates and that the broken fixture reports exactly its expected errors ([[tileset]]). Headless; part of `check`. |
| `adjacency-check` | import | Checks tile rotation expansion and the adjacency bitsets: socket matching rules, a hand-computed quarter turn against Godot's basis, the worked example, exclusions both ways, the fixture tileset's table, the word layout past 64 tiles, pairwise agreement and symmetry; prints build times and the fixture dump ([[tileset]]). Headless; part of `check`. |
| `adjacency-dump` | import | Prints the rotated tiles and adjacency table of a tileset: `mise run adjacency-dump <res://…tres>`; exits 1 with the validation errors of an invalid one ([[tileset]]). |
| `tiles-check` | import | Validates a tileset beyond its format: `mise run tiles-check <tileset.tres> [--max-contradiction-rate R] [--runs N] [--seed N]`. Fails on dead sockets, directions with no allowed tile, tiles unreachable from air and solid, `Ns` faces whose mesh profile is not mirror-symmetric, a tile never placed in 100 runs on a 6³ grid, or a contradiction rate above the threshold (default 0.5); prints the placement histogram ([[socket-adjacency#Validation]]). Headless. |
| `tiles-check-fixtures` | import | Self-test of `tiles-check`: the fixture tileset passes, the dead socket fixture fails with exactly its expected categories, placement is deterministic per seed, and the mesh symmetry check passes, fails and skips built meshes ([[tileset]]). Headless; part of `check`. |
| `tileset-build` | import | Regenerates `resources/tilesets/placeholder.tres` from `resources/tilesets/placeholder_builder.gd`, the box-only placeholder tileset ([[tileset#The placeholder tileset]]). Headless. |
| `tileset-build-check` | import | Builds the placeholder tileset into a temporary file and fails when the committed resource differs (script ids aside) or when it misses a tile family ([[tileset]]). Headless; part of `check`. |
| `tiles-check-placeholder` | import | `tiles-check` on the placeholder tileset with the default options ([[socket-adjacency#Worked example: the placeholder tileset]]); `--quick` places 20 runs instead of 100. Headless; part of `check`. |
| `solver-check` | import | Checks `SectorSolver` on the placeholder tileset: unconstrained 8³ solves at seeds 0 to 4 succeed at the first attempt, repeat byte for byte on a second instance, allow every adjacency and match [[solver_reference_seed0]]; the entropy flag and `domains` restrictions work; restarts, the all-solid degradation, inconsistent records failing fast, consistent records holding and fixed faces. Prints the tile histogram, steps and time per seed, a 24³ solve with restarts and the outcome of 20 real stratum sectors at seed 0, and requires the 20 real stratum sectors of seeds 1 to 4 to reach an attempt; `--update` rewrites the reference; `--quick` runs the reduced sample ([[solver]]). Headless; part of `check`. |
| `solver-real` | import | Explains real sectors that fail before an attempt: `mise run solver-real [--seeds 0,1,2,3,4] [--count N] [--no-solve] [--min-solved N]`. For each world seed runs the real stratum sectors `solver-check` samples, prints the rejected record pair or a minimal set of records whose starting domains empty a cell, solves the rest and prints solved, degraded and failed counts per seed; exits 1 when a sector fails before an attempt or a seed solves fewer than `--min-solved` ([[solver]]). Headless. |
| `solver-sector` | import | Runs the fill pipeline for one real sector: `mise run solver-sector <x> <y> <z> [--seed N]`. Prints the sector type, records, outcome, attempts, restarts, pre-collapsed cells, time and a family histogram; exits 1 when the domains or the solve fail or a solved result breaks a record or an adjacency ([[solver]]). Headless. |
| `boundary-check` | import | Checks the face-first sector boundaries on the placeholder tileset: 100 random faces at seeds 0 to 4 are identical from the sector and from its neighbour on separate instances, a sector repeats on a second instance, 50 unconstrained 8³ and 2 24³ adjacent pairs all solve with no socket mismatch across the shared face or inside, and 50 real 24³ pairs whose records build have none in every pair that solved; prints the outcome counts and the corner, edge, face and interior time of a 24³ sector; `--quick` runs the reduced sample ([[solver]]). Headless; part of `check`. |
| `wfc-bench` | import | Benchmarks the solver: `mise run wfc-bench [--seed N] [--sectors N] [--boundaries]`. Solves the first N (default 10) stratum sectors walking outwards from the origin through the real pipeline and the same sectors unconstrained, with `--boundaries` also cold through `SectorBoundaries.solve_sector`, and prints markdown tables per sector and a summary with a verdict against the 1 s threshold of #138 ([[solver]], [[maintaining#Benchmarking the solver]]). Headless; not part of `check`. |
| `jobs-check` | import | Checks threaded sector jobs: `mise run jobs-check [--quick] [--boundaries] [--seed N] [--workers N]`. Tests priority by distance, duplicate requests, cancel and clear; solves the 27 sectors of the 3³ block around the origin on the WorkerThreadPool while polling once per frame, and fails when a poll blocks the main thread over 4 ms, a started task is not waited on, a result is missing, duplicated or differs from the same sector solved on the main thread, or the log shows an error, a warning or a `WorkerThreadPool` line; prints a row per sector, poll times and throughput ([[sector-jobs]]). Headless; `check` runs `--quick` (8 sectors) and `check-full` all 27, each after its parallel tasks. |
| `gridmap-check` | import | Checks `SectorGridMap`: the GridMap orientation index of each yaw quarter turn both ways, an asymmetric mesh placed at each turn whose geometry and collision end up in the hand-computed corner, the placeholder mesh library (one item and trimesh shape per prototype with a mesh, none for air) and each placed stair rotation high on the face `TileLibrary` gives its rock socket ([[placement#Orientation indices]]). Headless; part of `check`. |
| `multimesh-check` | import | Checks `SectorMultiMesh`: a stair turned once at local cell (3, 5, 7) gives exactly the 12 hand-computed `MultiMesh.buffer` floats and its mesh faces turned and moved by hand as collision; two solid cells side by side drop their 4 shared triangles; a solved 8³ sector placed as a `SectorGridMap` and as a `SectorMultiMesh` has the same world-space instance transforms per mesh and the same hits for 768 rays and 64 capsule casts; a real sector solved through `SectorJobs` with `build_placement` carries placement data equal to a main-thread build; prints the worker build time and the main-thread time of placing that sector both ways ([[placement#Buffer layout]]). Headless; part of `check`. |
| `multimesh-draw-calls` | import | Under `xvfb-run` with the OpenGL renderer, like `shot`: checks that `set_instance_transform` writes the layout `SectorMultiMesh.write_instance` writes, then places each solved sector of the 3³ block at seed 0 as a GridMap and as MultiMeshes in front of a camera and prints a markdown table of draw calls (`RENDER_TOTAL_DRAW_CALLS_IN_FRAME`), build time and collision triangles; fails when a MultiMesh sector takes more draw calls than it uses meshes ([[placement#Measurements]]). Not part of `check`: it needs a renderer. |
| `walk-check` | import | Walks the `Player` capsule through a sector placed as a `SectorMultiMesh` and then as a `SectorGridMap` (`--placement multimesh` or `gridmap` for one) one physics step per frame (`--fixed-fps 60`) and fails when it falls 0.5 m below the floor, does not reach a cell within 240 frames or ends over 1 m from the goal. Default: a hand-laid course of placeholder tiles; `mise run walk-check --sector x,y,z [--seed N]` solves a real sector and walks portal to portal along the edge rasteriser's records ([[placement]]); `--all-solving [--seed N]` walks every stratum sector within 3 sectors of the origin that solves and reports how many walk. Headless; the course is part of `check`. |
| `streaming-check` | import | Checks streaming: `mise run streaming-check [--quick] [--radius R]`. Walks a frozen `Player` capsule 4 m per step along +x through the centres of sectors (x, 0, 0) of the streamed world at seed 0 (`SectorJobs` and `SectorStreamer`, radius 1): a warm-up to sector 1 and back sets the memory baseline, then out to sector 10 (2 with `--quick`) and back. A step waits until everything within the radius is placed and the collision around the next position is there. Fails when a frame after the first placements lacks the player's sector or the collision around its feet, a step lacks the player's sector, its 6 face neighbours or any sector within the radius or keeps one beyond radius + 1, walking back solves instead of hitting the cache, static memory or the object count after walking back is more than 10 % off the baseline or the node count differs, a frame exceeds 33 ms, or the log shows an error or a warning; prints the loaded-sector timeline, the memory table, the longest frame with a breakdown of frames over 16 ms and the streamer's longest update, refresh, placement and unload ([[sector-streaming]]). Headless; `check` runs `--quick` and `check-full` the full walk, each alone after `jobs-check`. |
| `collision-cook-bench` | import | Measures adding one real sector's trimesh collision on the main thread: `mise run collision-cook-bench [--sector x,y,z] [--seed N] [--chunk-cells N]`. Merged through nodes and through `PhysicsServer3D`, the shape created on a worker, one body per chunk, the chunks as shapes of one body and through `SectorMultiMesh.add_collision_chunk` (8 bodies); fails when a strategy's rays hit other heights than the merged shape; prints the table of decision #175 ([[sector-streaming#The collision cook strategy]]). Headless, not part of `check`. |
| `panel-check` | import | Clicks every kind of script-backed widget in the skeleton viewer's tweak panel and checks each setter ran ([[tweak-ui]]). Headless; part of `check`. |

## Check tiers

`check` is the tier for every pull request and stays under 2 minutes;
`check-full` runs the same tasks at full size on pushes to `develop` and
nightly, and before merging a change to the grammar, the graph, the
rasteriser, the tileset or the solver. Six tasks take a
`--quick` flag that shrinks their sample; the cheap reference comparisons
(skeleton histogram, solver seed 0 digest) and every other task run
unchanged in both tiers. Each quick run still fails on the same conditions,
only over fewer cases.

| Task | `check` (`--quick`) | `check-full` |
| --- | --- | --- |
| `graph-connectivity` | the default variant only, seeds 0 to 2, strict 5³ windows at every second offset (216 per seed) | all five variants, seeds 0 to 9, all 1 331 strict windows per seed |
| `raster-check` | 40 sectors per seed 0 to 4 (200) | 200 sectors per seed (1 000) |
| `tiles-check-placeholder` | 20 placement runs | 100 placement runs |
| `solver-check` | 8³ solves at seeds 0 and 1 (seed 0 against the reference), the entropy, domain, restart, record and face checks; no 24³ solve, no real sectors | 8³ solves at seeds 0 to 4, the same checks, a 24³ solve and 20 real stratum sectors |
| `boundary-check` | 4 faces per seed 0 to 4 (20), the determinism check, 10 unconstrained 8³ pairs | 20 faces per seed (100), the determinism check, 50 unconstrained 8³ pairs, 2 unconstrained and 50 real 24³ pairs, level timings |
| `jobs-check` | the queue checks and the first 8 sectors of the 3³ block | the queue checks and all 27 sectors |
| `streaming-check` | warm-up, then 2 sectors out and back (about 55 s) | warm-up, then 10 sectors out and back (about 130 s) |
| `skeleton-histogram`, `skeleton-stats`, `graph-check`, the tileset and adjacency checks | unchanged, a few seconds each | unchanged |

`smoke` matches the patterns case-sensitively. Its allow-list for known
benign lines (the `allow` array in the task) is empty, because the run log is
clean. Headless Godot uses the dummy renderer, which never compiles shaders,
so the ray-march shader produces no messages there and shader errors are not
caught by `smoke`.

The window-based checks need a real rendering device because they read back
rendered frames, so they do not run in CI. To get an image of a scene without
a window, use `shot`, which renders inside Xvfb ([[screenshots]]). Every
other tool task runs `--headless`.

The `release:*` tasks follow one rule: `develop` always carries the NEXT
version. `release:release` tags the version already in `project.godot` and
never edits it before tagging; right afterwards `release:bump patch` moves
`develop` on ([[0014-develop-is-always-the-next-version]]). The file-changing
release tasks print each command as `+ command` and accept `--dry-run`, and
so do the `wt:*` and `pr:*` tasks that change anything.

## Tool scripts

Each script `extends SceneTree` and is started with
`godot --path . --script res://scripts/tools/<name>.gd` by its task. The
checks print one `ok` or `FAIL` line per expectation and a final summary, and
exit with status 1 on any failure.

| Script | Task | What it does |
| --- | --- | --- |
| [hash_vectors.gd](../../scripts/tools/hash_vectors.gd) | `hash-vectors` | Prints `hash3_u` and `hash3` for eight fixed inputs as a Markdown table; must match [[hash_vectors]]. |
| [registry_dump.gd](../../scripts/tools/registry_dump.gd) | `ui-params` | Builds a parameter registry on the main scene's ray-march material and prints every group and parameter with type, default, range and step. |
| [seed_check.gd](../../scripts/tools/seed_check.gd) | `seed-check` | Checks seed parsing and clamping, that `seed_changed` fires once per change, that the uniform follows the seed, that a different seed renders a different frame and the same seed an identical one (grain and HUD turned off), and the seed field and R key. |
| [screenshot_check.gd](../../scripts/tools/screenshot_check.gd) | `screenshot-check` | Takes a HUD screenshot and checks the file name, folder and resolution, and that the HUD and panel were hidden for that frame and shown again afterwards; then presses H, F1 and P and checks they toggle the HUD and hint and save a screenshot, and are ignored while the seed field has focus. |
| [preset_check.gd](../../scripts/tools/preset_check.gd) | `preset-check` | Changes every parameter, the seed and the camera, saves a preset, loads the defaults, reloads the preset from disk and compares everything bitwise, including a restart and delete; also tests name validation and exact float reading. Uses a scratch `user://preset_check` folder. |
| [code_map_check.gd](../../scripts/tools/code_map_check.gd) | `code-map-check` | Collects every file under `scripts/`, `shaders/` and `scenes/` (except `.uid` and `.import`), scans the links in `docs/code/*.md`, and fails if a file is linked from no note or from more than one, or if a link points at a missing source file. |
| [shot.gd](../../scripts/tools/shot.gd) | `shot` | Loads a scene with saved presets off, sets the seed, the FreeFlyCamera pose and registry params by name (typed by the registry), waits the frames, grabs a frame with `Hud.grab_frame` (HUD and panel hidden unless `--ui`), checks the resolution and saves the PNG; prints `shot: <problem>` and exits 1 on any error. |
| [skeleton_histogram.gd](../../scripts/tools/skeleton_histogram.gd) | `skeleton-histogram` | Counts the sector types of the 9 × 9 × 9 sectors around the origin for seed 0, per layer and in total; checks `sector_type` returns a type for 2 000 hashed random cells and the int32 extremes, also under an out-of-range grammar, and that two evaluations (and a fresh `Skeleton`) agree; then compares the table rows with [[skeleton_histogram_seed0]], or rewrites that file with `--update`. |
| [skeleton_stats.gd](../../scripts/tools/skeleton_stats.gd) | `skeleton-stats` | Samples 20 random 5 × 5 × 5 sector regions for each seed 0 to 4 and prints the type fractions, the mean vertical shaft run (each run followed past the region to its full length), the non-solid components per region (6-connected union-find), the cavity clusters per region and the stratum run lengths along x, z and y; fails below 3 sectors of shaft run or 2 components. `measure()` and `print_stats()` are static, so a scratch script can compare grammars. |
| [graph_check.gd](../../scripts/tools/graph_check.gd) | `graph-check` | For each seed 0 to 4, draws random adjacent sector pairs (within ±100 000 sectors) until 100 have two open sectors and 20 a solid one; checks `portal(a, b)` equals `portal(b, a)` and the portal of a fresh graph exactly, lies on the shared face at least one cell inside its edges, on the 2 m grid and, on a vertical face, at the floor level of the lower sector's interior node; that the x and z edge portals around 20 sample sectors, tunnels included, lie at the lower sector's hub level; that solid pairs and 8 kinds of non-adjacent pair (same sector, diagonals, two or three apart) return null; that both sectors' interior nodes carry their type and lie inside the margin on the grid at a floor level; and that `nodes_in_region` of a 3³ region returns exactly its open sectors in order. |
| [graph_connectivity_check.gd](../../scripts/tools/graph_connectivity_check.gd) | `graph-connectivity` | For each graph variant in `VARIANTS` (boundary scheme and `void_wall_tunnels_last`, default marked) and each seed 0 to 9, takes the 5³ regions (15³ sectors) at a hashed offset within ±10 000 regions, and again centred on the lower x wall of the first chasm along +x, and computes their edges and the boundary edges between them on a caching skeleton; checks every edge joins face-adjacent sectors in order with the kind `edge_kind` gives and a portal on their face (equal to `portal` for open pairs), that a fresh graph gives identical region edges, that regions whose open sectors are open-connected with no solid terminal have no tunnel, and that `edges_for_sector` matches for 40 sectors. Then union-find per window, only over edges with both ends inside: fails unless all 125 region-aligned 3³, 64 6³ and 27 9³ windows have one component of non-solid sectors; prints how many of the 1 331 5³ windows at every offset of the random sample have one, next to what every open-open adjacency without tunnels would reach, the tunnel fraction, the tunnels with a chasm or cavity end (and how many lie on boundary faces), the faces left without an edge, the edge kinds and the mean vertical run (stacked stair and ladder edges), then one table row per variant and sample. |
| [raster_check.gd](../../scripts/tools/raster_check.gd) | `raster-check` | Checks `merge` is commutative and idempotent over every pair of families and orientations. For each seed 0 to 4, draws 200 sectors within ±100 000 (salt 904) and rasterises each on a caching skeleton and on a fresh graph, which must agree record for record; checks one record per cell inside 0..23, that every record of each edge's walk merges into the final record without conflict, that both endpoint sectors of every edge hold a portal opening at its portal cell, that no edge is rejected, that no record lies directly above or below a stair cell, that the records form one 26-connected group, and steps through each walk from the hub to the portal: flat to flat only at one level, onto, along and off stairs in their orientation, on and off ladders. Prints the family counts, the mean records and stair cells per sector and the stair, ladder and landing cells of the walks. |
| [tileset_check.gd](../../scripts/tools/tileset_check.gd) | `tileset-check` | Parses valid and invalid socket strings (`3s`, `3f`, `5_2`, `0i`; `03`, `3S`, `5_4`, a bare `5` on a vertical face) on all six faces and compares id, symmetric, flipped and rotation, and the string printed back; validates a `BoxMesh` prototype of every `TileFamily` and of `FAMILY_NONE` and rejects families −2 and 7; loads `tests/fixtures/tilesets/fixture_tileset.tres` and checks it validates, its prototypes, `find`, meshes, weight, rotations and parsed sockets, and that `missing_families` and `validate(true)` report the six families other than floor; loads `broken_tileset.tres` and compares its `validate()` list with the 14 expected lines in order. |
| [adjacency_check.gd](../../scripts/tools/adjacency_check.gd) | `adjacency-check`, `adjacency-dump` | Checks `TileLibrary.sockets_match` and `partner_key` on 28 horizontal and vertical pairs; turns `1 2s 5_0 6_3 3f 4` once and twice against hand-computed rows, four times and back, and compares `QUARTER_TURN` with each face normal turned by `Basis(Vector3.UP, PI / 2)`; rebuilds the A/B/C worked example of [[socket-adjacency]]; checks an exclusion between a prototype and all four rotations of another in all six directions; loads the fixture tileset and compares its 5 tile labels, weight, sockets and all 30 bitsets; builds 66 tiles and checks two words per entry, the bits at 63 and 64 and out-of-range arguments; checks every table equals the pairwise socket rule and is symmetric. Prints build times and the fixture dump. With `-- --dump <path>` it only prints that tileset's dump. |
| [tiles_check.gd](../../scripts/tools/tiles_check.gd) | `tiles-check`, `tiles-check-fixtures` | Loads a tileset, builds its `TileLibrary` (stopping at validation errors) and reports, one `FAIL  <category>: <message>` line each: sockets whose partner no tile shows on the opposite face, tiles with an empty allowed bitset in a direction, tiles a breadth-first search from every rotation of air and solid does not reach, `Ns` faces whose mesh vertices within 0.01 m of the face plane have no mirror image across the face's centre line within 0.001 m (`BoxMesh` via `get_mesh_arrays`, `ArrayMesh` via `surface_get_arrays`, warning for no vertex data), tiles never placed and a contradiction rate above `--max-contradiction-rate`. The placement fills a 6³ grid `--runs` times: cells in `hash3_u` order, weighted `hash3` choice, bitset AC-3 propagation, restart with the next attempt seed on a contradiction, at most 8 restarts; prints tile counts, shares and the rate. With `-- --self-test` it checks both fixtures, determinism per seed, the threshold and four built meshes. |
| [solver_check.gd](../../scripts/tools/solver_check.gd) | `solver-check` | Builds the placeholder `TileLibrary`; for seeds 0 to 4 solves an 8³ grid at sector (0, 0, 0) on two `SectorSolver` instances and requires success at the first attempt, identical cells and steps, and no face-neighbour pair `is_allowed` rejects, printing steps, queue pops, time, a SHA-256 prefix and the tile histogram. Repeats the check once with `use_entropy`. Restricts one cell to solid through `domains` and requires it in the result and in `precollapsed`, then restricts a floor directly above air and requires `FAILED` with no attempt. Solves seed 36 with 1 attempt (degraded, all solid, repeated on a second instance) and with the default 8 (solved at attempt 2, repeated). Feeds `SectorDomains` two records at one cell, a record outside the grid, a stair with orientation up and a tunnel over a bridge and requires each error; a bridge two cells under a tunnel must pass the builder and fail in the solver with no attempt. Builds a floor walk, a 3-cell stair run and a portal opening on an 8³ stratum grid and requires every solved result of seeds 0 to 9 to keep all records and adjacencies. Fixes the +y face to solid with one free entry and requires rock-accepting tiles on top, and a wrong face size to be an error. Compares the seed 0 SHA-256 of `cells.to_byte_array()` with the `sha256:` line of [[solver_reference_seed0]], or rewrites that file with `--update`. Then solves 24³ at seed 0 with restarts, and runs the first 20 stratum sectors with records within ±1 000 (salt 905) at seed 0 through `SolverSectorRun`, printing each outcome and the solved, degraded, failed and rejected counts and mean attempts; a solved one must keep its records and adjacencies. Last, for seeds 1 to 4 builds the starting domains of the 20 real stratum sectors and requires each to propagate without an empty cell. |
| [solver_real.gd](../../scripts/tools/solver_real.gd) | `solver-real` | Parses `--seeds`, `--count`, `--no-solve` and `--min-solved`; per seed takes `SolverSectorRun.real_sectors`, prints the `SectorDomains` error of a rejected sector, or for one whose starting domains empty a cell shrinks its records by dropping halving chunks while the rest still fail and prints the minimal set relative to its first record; otherwise solves and prints the outcome. Prints one count line per seed. |
| [solver_sector.gd](../../scripts/tools/solver_sector.gd) | `solver-sector` | Parses `<x> <y> <z> [--seed N]`, builds the placeholder library and a graph for the seed, runs `SolverSectorRun` and prints the type, records, outcome, attempts, restarts, pre-collapsed cells, steps, solve and domain times and the family histogram; for a solved sector counts broken records and forbidden adjacencies, for a degraded one prints the last contradiction cell. |
| [solver_sector_run.gd](../../scripts/tools/solver_sector_run.gd) | `solver-check`, `solver-sector`, `solver-real` | `class_name SolverSectorRun`: one pipeline pass for a real sector (type, records, `SectorDomains.build`, `solve`, reusing a given solver) with `broken_records`, `bad_adjacencies` and the family `histogram` (tiles of no family by prototype name); `real_sector` and `real_sectors`, the sample of stratum sectors with records within ±1 000 (salt 905); and `empty_cell`, an AC-3 propagation of starting domains that returns the first emptied cell without solving. |
| [boundary_check.gd](../../scripts/tools/boundary_check.gd) | `boundary-check` | Builds the placeholder library. For seeds 0 to 4 draws 20 sectors within ±1 000 and a face direction each (salt 906), computes `face(s, dir)` on one `SectorBoundaries` with that seed's rasteriser and, on a second instance, another face of `s + dir` and then `face(s + dir, dir ^ 1)`, and requires identical 24 × 24 layers; prints the solved, degraded and failed face pieces and the mean cold face time. Composes an 8³ sector on two instances, the second after a seed change and another face. Solves 50 random adjacent 8³ pairs and 2 24³ pairs unconstrained, and 50 24³ pairs whose `SectorDomains.for_sector` both build, each pair neighbour first on its own instance; for pairs where both sectors solved counts forbidden pairs across the shared face and inside (`SolverSectorRun.bad_adjacencies`) and broken records, all must be 0, and every unconstrained pair must solve. Prints the per-level time of a cold unconstrained sector (0, 0, 0) and of a real one. |
| [wfc_bench.gd](../../scripts/tools/wfc_bench.gd) | `wfc-bench` | Parses `[--seed N] [--sectors N] [--boundaries]` and builds the placeholder library and a graph for the seed. Walks shells of growing Chebyshev radius from sector (0, 0, 0), z, y, x ascending inside a shell, and keeps the first N stratum sectors. Runs each through `SolverSectorRun` on one reused 24³ solver (a rejected sector's time is its domain build, a solved one's includes it), then solves each unconstrained at the same seed and sector, and with `--boundaries` clears a `SectorBoundaries` cache and composes each sector, summing attempts, steps and propagations over its pieces. Prints one markdown table per run (sector, records, outcome, attempts, steps, propagations, time, error or level times), a summary table (searched, mean and max time over searched sectors, mean attempts, contradictions, degraded, failed before an attempt) and a verdict line from the real pipeline, or from the unconstrained solves when no real sector ran an attempt. |
| [jobs_check.gd](../../scripts/tools/jobs_check.gd) | `jobs-check` | Parses `[--sectors N] [--seed N] [--workers N] [--boundaries]`, builds the placeholder library and adds a `SectorJobs` with processing disabled, so the script polls it itself. Queue checks with one slot and focus (100, 0, 0): (99, 0, 0) starts before (40, 0, 0) requested first, a duplicate is refused, a queued and a running sector cancel, a re-request and `clear` with `wait_all` emit nothing and wait on every task. Then requests the first N sectors of the 3³ block around the origin (nearest first, then z, y, x) and times every `poll` from `_process` with `Time.get_ticks_usec`; when idle, solves each sector again on the main thread through `SolverSectorRun` on one graph (or one `SectorBoundaries`) and compares outcome, attempts, degraded flag and cells. Prints a markdown row per sector with type, outcome, attempts, worker and main-thread time, the outcome counts, poll count, mean and max, and the throughput and speed-up. |
| [gridmap_check.gd](../../scripts/tools/gridmap_check.gd) | `gridmap-check` | Checks `get_orthogonal_index_from_basis` and `get_basis_with_orthogonal_index` against `SectorGridMap.YAW_ORIENTATIONS` for 0 to 3 quarter turns; places a 0.8 × 2 × 0.4 m box offset to the `+x -z` corner at each turn in one map and checks `get_cell_item_basis` moves its centre to the hand-computed corner and that a ray cast down hits it there and not in the mirrored corner; builds the placeholder mesh library and checks item names, meshes and one `ConcavePolygonShape3D` each; places a 2³ result of the four stair rotations under air with `place` and checks items, orientations, the map position and a ray on the stair's rock-socket side hitting above 0.5 m and one opposite below -0.3 m. |
| [multimesh_check.gd](../../scripts/tools/multimesh_check.gd) | `multimesh-check`, `multimesh-draw-calls` | Builds the placeholder library and its `prototype_faces`. Headless: checks the layout and collision of one stair and the solid face culling on 8³ grids of air, solves an 8³ sector (first solving solve seed), places it at sector (1, -1, 2) as a GridMap, keys `get_meshes()` by prototype and rounded world transform, casts rays down, along +x and +z and capsules down through a 16 × 16 pattern of columns, frees it, does the same with a `SectorMultiMesh` reading its buffers back, and compares; runs one `SectorJobs` task for sector (-1, 1, -1) and compares its placement with a main-thread `build`, then times placing both ways to the end of the next frame. With `--render`: the renderer layout check and the draw call table. |
| [walk_check.gd](../../scripts/tools/walk_check.gd) | `walk-check` | Parses `[--sector x,y,z \| --all-solving] [--seed N] [--placement multimesh\|gridmap]`. With `--all-solving` collects the stratum sectors within `ALL_SOLVING_RADIUS` (3) of the origin with two kept horizontal portals and records that build, solves them in one `WorkerThreadPool` group task and walks each solved one in turn on every placement, printing how many sectors walk portal to portal on all of them. Without a sector lays the course into a 24³ result (air, floor and stair cells with solid under each and at each stair's high end); with one, solves it with `SectorJobs.solve_sector` and builds the route from `EdgeRasteriser.rasterise` (first kept horizontal edge reversed, then the last one). For each placement (MultiMesh first) places the cells with `SectorMultiMesh.build` and `place` or with `SectorGridMap`, adds a `Player`, settles 30 frames and steers `scripted_direction` at each cell centre; prints the route with tiles, frames per cell, steps climbed, the final distance, and on failure the tiles in the columns at and ahead of the feet. |
| [streaming_check.gd](../../scripts/tools/streaming_check.gd) | `streaming-check` | Parses `[--sectors N] [--radius R] [--seed N]`, switches off the headless frame pacing sleep (`OS.low_processor_usage_mode_sleep_usec`) so a frame's wall time is its work, adds a `SectorJobs`, a `SectorStreamer` and a `Player` with `walking` off and drives a phase machine from `_process`: first placements (not timed), warm-up out and back, baseline after settling and 10 flush frames, walk out, walk back, final. Each frame checks the player's sector and `is_collision_ready_at` and records the frame interval (with the streamer's last update, refresh, frees, placements, chunks, the jobs poll and physics time for frames over 16 ms); each step checks the face neighbours, the radius and nothing beyond radius + 1. Prints a markdown timeline row per sector boundary crossed and per settle, then the checks, the memory table and the frame summary. |
| [collision_cook_bench.gd](../../scripts/tools/collision_cook_bench.gd) | `collision-cook-bench` | Parses `[--sector x,y,z] [--seed N] [--chunk-cells N] [--repeat N]`, solves the sector with placement data merged and chunked, switches off the frame pacing sleep and times each strategy from the first call to the end of the frame after the last, with the longest frame between (process frame intervals) and the freeing time. After each, casts a ray down through every 2 m column, 0.37 and 0.61 cells in from the corner so no ray runs along a triangle edge, and compares the hit heights with the merged shape's within 1 mm. Prints the table. |
| [panel_check.gd](../../scripts/tools/panel_check.gd) | `panel-check` | Opens the skeleton viewer's tweak panel with Tab and pushes mouse clicks: on the `show_stratum` box and its label, a press without release on `follow_camera`, the up arrows of the `radius`, `shaft_probability` and `solid_wall_grid` spin boxes and the `sector_size` slider; checks the viewer or grammar value changed each time. |

## How to run or check it

- `mise run check` before every pull request; it must pass. Run
  `mise run check-full` as well when a change touches the grammar, the
  walkable graph, the rasteriser, the tileset or the solver, since the
  statistical tasks only see their full sample there.
- Run the check matching what you changed: `preset-check` for presets and the
  registry, `seed-check` and `screenshot-check` (with a display) for the seed
  and HUD, `hash-vectors` for the hash, `skeleton-histogram` and `skeleton-stats` for the
  sector grammar, `graph-check`, `graph-connectivity` and `raster-check` for the walkable graph, `tileset-check` for tile resources, `adjacency-check` for rotation expansion and the adjacency table, `tileset-build` then `tiles-check-placeholder` for the placeholder tileset, `solver-check` for the sector solver, `boundary-check` for the sector borders, `wfc-bench` for solver speed, `jobs-check` for threaded sector jobs, `gridmap-check`, `multimesh-check`, `multimesh-draw-calls` and `walk-check` for placement and the player, `streaming-check` and `collision-cook-bench` for streaming, `panel-check` for the tweak panel widgets, `code-map-check` after adding, moving or removing a source file or
  editing these notes.
- Need to see a change? `mise run shot scenes/main.tscn build/shots/main.png`
  renders it without a window.

## References

- [[0001-all-automation-through-mise]]: why every command is a mise task.
- [[ci-and-export]]: the CI workflows and export tasks in context.
- [[releasing]]: the release tasks in the release process.
- [[maintaining]]: the worktree and pull request tasks in the maintainer loop.
- [[screenshots]]: how `shot` renders without a window, and troubleshooting.
- [[tweak-ui]], [[hash]], [[skeleton]], [[walkable-graph]], [[solver]], [[placement]]: what the checks test.
- [[CONVENTIONS]]: keeping these notes in step with the code.
